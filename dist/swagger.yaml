openapi: 3.0.0
# Added by API Auto Mocking Plugin
servers:
  - description: SwaggerHub API Auto Mocking
    url: https://thang-codersx-express.herokuapp.com/api
info:
  description: Test APIs document Swagger
  version: "1.0.0"
  title: APIs document Swagger
  termsOfService: 'http://swagger.io/terms/'
  contact:
    email: apiteam@swagger.io
  license:
    name: Apache 2.0
    url: 'http://www.apache.org/licenses/LICENSE-2.0.html'
tags:
  - name: Book
    description: Operations about book
paths:
  /book:
    post:
      tags:
        - book
      summary: Create Book
      description: This can only be done by the logged in user.
      operationId: createUser
      responses:
        default:
          description: successful operation
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Book'
        description: Created book object
        required: true
components:
  schemas:
    Book:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
      xml:
        name: Book
  requestBodies:
    UserArray:
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '#/components/schemas/Book'
      description: List of book object
      required: true
  securitySchemes:
    test_auth:
      type: oauth2
      flows:
        implicit:
          authorizationUrl: 'https://thang-codersx-express.herokuapp.com/api'
          scopes:
            'write:users': modify your account
            'read:users': read your information
    api_key:
      type: apiKey
      name: api_key
      in: header